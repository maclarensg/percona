# Example Terraform variables file
# Copy this to terraform.tfvars and update with your values

vault_token = "your-vault-root-token-here"

postgres_cluster_name = "pg-cluster"
postgres_namespace    = "percona"
